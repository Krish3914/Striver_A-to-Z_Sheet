// array is the data structure contain similar data type elements.
// array can contain any of the data type but all element should be of similar data-type.

// maximum size of the array that can be declared is:
// int arr[pow(10,6)]   when declared inside the main()
// int arr[pow(10,7)]   when declared globally

// by default indexing of array starts from 0 to arr.size()-1
// array elements stored at continuous memory allocation


